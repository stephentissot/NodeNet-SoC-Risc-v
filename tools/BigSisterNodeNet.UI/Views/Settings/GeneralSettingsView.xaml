<UserControl x:Class="BigSisterNodeNet.UI.Views.Settings.GeneralSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BigSisterNodeNet.UI.Views.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Instrument name" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
        <TextBox Grid.Row="0" x:Name="instrumentName"
                 Margin="150,0,0,5"
                 Text="{Binding Node.InstrumentName, UpdateSourceTrigger=LostFocus}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>

        <TextBlock Grid.Row="1"
                   Text="Node address" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
        <TextBox Grid.Row="1"
                 Margin="150,0,0,5"
                 Text="{Binding Node.Address}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}" IsEnabled="False"/>

        <CheckBox Grid.Row="2"
                  Content="RS485 terminator"
                  IsChecked="{Binding Node.Rs485Terminator}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
        <Button Grid.Row="3" Width="200" Content="Reboot Node" Command="{Binding RebootCommand}">
        </Button>

    </Grid>
</UserControl>
