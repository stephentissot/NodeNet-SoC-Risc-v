<UserControl x:Class="BigSisterNodeNet.UI.Views.Settings.NetworkSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:BigSisterNodeNet.UI.Views.Settings"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:BigSisterNodeNet.UI.Models.Settings"
             d:DataContext="{d:DesignInstance Type=vm:NetworkSettingsViewModel}"
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin ="20" IsEnabled="{Binding IsSaving,Converter={StaticResource InverseBooleanConverter}}">
        <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <CheckBox Grid.Row="0" Content="Enable Wifi" IsChecked="{Binding Enabled}" />
            <TextBox Grid.Row="2" Text="{Binding WifiSSID, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="SSID" IsEnabled="{Binding Enabled}" />
            <TextBox Grid.Row="4" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"    materialDesign:HintAssist.Hint="Password" IsEnabled="{Binding Enabled}"/>
            <Button Grid.Row="6" Command="{Binding GoSave}" Content="Apply Network settings" Width="250" IsEnabled="{Binding IsUpdated}" />
            <TextBlock Grid.Row="8" Text="{Binding LastError, Mode=OneWay}" />

        <Grid Visibility="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}}" Background="#80000000">
            <ProgressBar Width="250" Height="6" IsIndeterminate="True" VerticalAlignment="Center"/>
        </Grid>

    </Grid>

</UserControl>
