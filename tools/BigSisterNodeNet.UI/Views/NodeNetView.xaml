<UserControl x:Class="BigSisterNodeNet.UI.Views.NodeNetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:models="clr-namespace:BigSisterNodeNet.Core.Models;assembly=BigSisterNodeNet.Core"
             xmlns:local="clr-namespace:BigSisterNodeNet.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding HardwareType}" FontWeight="Bold" VerticalAlignment="Center" TextAlignment="Left" Margin="5">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HardwareType}" Value="{x:Static models:HardwareType.Undefinded}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <CheckBox Grid.Column="1" Content="Show all nodes" IsChecked="{Binding SelectAllNodeType}" VerticalAlignment="Center" HorizontalAlignment="Left">
                <CheckBox.Style>
                    <Style TargetType="CheckBox">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HardwareType}" Value="{x:Static models:HardwareType.Undefinded}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </CheckBox.Style>
            </CheckBox>
            <ComboBox Grid.Column="3" Background="Black" Width="100" ItemsSource="{Binding Coms}" SelectedItem="{Binding SelectedCom}" Height="40" x:Name="comboComPort" VerticalAlignment="Center" VerticalContentAlignment="Center" />
            <Button Grid.Column="4" x:Name="buttonConnect" Width="100" Height="30" Margin="0,0,0,0" Content="Connect" Command="{Binding ConnectCommand}" VerticalAlignment="Center" />
        </Grid>

        <DataGrid ItemsSource="{Binding Nodes}" AutoGenerateColumns="False" IsReadOnly="True" Grid.Row="1">
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                    <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTemplateColumn  Header="" Width="40">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <materialIcons:MaterialIcon Width="24" Height="24" Margin="2">
                                <materialIcons:MaterialIcon.Style>
                                    <Style TargetType="materialIcons:MaterialIcon">
                                        <Setter Property="Foreground" Value="Red"/>
                                        <Setter Property="Kind" Value="LanDisconnect"/>
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform/>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Node.IsOnline}" Value="True">
                                                <Setter Property="Foreground" Value="LimeGreen"/>
                                                <Setter Property="Kind" Value="LanConnect"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Heartbeat}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ColorAnimation Storyboard.TargetProperty="(Foreground).(SolidColorBrush.Color)" To="LightGoldenrodYellow" Duration="0:0:0.25" AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </materialIcons:MaterialIcon.Style>
                            </materialIcons:MaterialIcon>
                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Node" Binding="{Binding Node.InstrumentName}" />
                <DataGridTextColumn Header="#" Binding="{Binding Node.Address}" ElementStyle="{StaticResource ColumnCenteredText}">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn  Header="Master">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <materialIcons:MaterialIcon Width="24" Height="24" Margin="2">
                                <materialIcons:MaterialIcon.Style>
                                    <Style TargetType="materialIcons:MaterialIcon">
                                        <Setter Property="Foreground" Value="Red"/>
                                        <Setter Property="Kind" Value="{Binding MasterIcon}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Node.IsOnline}" Value="True">
                                                <Setter Property="Foreground" Value="LimeGreen"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding MasterHeartbeat}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ColorAnimation Storyboard.TargetProperty="(Foreground).(SolidColorBrush.Color)" To="LightGoldenrodYellow" Duration="0:0:0.5" AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </materialIcons:MaterialIcon.Style>
                            </materialIcons:MaterialIcon>
                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Device" Binding="{Binding Node.DeviceId}" />

                <DataGridTextColumn Header="Type" Binding="{Binding Node.HardwareType}" />


                <DataGridTemplateColumn Header="" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>

                            <ItemsControl ItemsSource="{Binding Features}" HorizontalAlignment="Right">

                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border ToolTip="{Binding Tooltip}" Margin="2">
                                            <materialIcons:MaterialIcon
                                            Kind="{Binding Icon}"
                                            Width="24"
                                            Height="24"
                                            ToolTip="{Binding Tooltip}"
                                            Foreground="White"
                                            Margin="2"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>

                            </ItemsControl>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="2" Text="{Binding CoreMessage}" VerticalAlignment="Center" TextAlignment="Right"/>
    </Grid>
</UserControl>
