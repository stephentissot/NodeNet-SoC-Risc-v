<UserControl x:Class="BigSisterNodeNet.UI.Views.Instruments.NodeNet_SOCProgrammingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BigSisterNodeNet.UI.Models.Instruments"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:NodeNet_SOCViewModel}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Margin="0,0,8,0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                       Text="Slot"/>
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding PlcSlotChoices}"
                      SelectedItem="{Binding SelectedPlcSlot}"
                      Margin="0,0,12,0"/>
            <Button Grid.Column="2"
                    Content="Upload"
                    Command="{Binding UploadPlcProgramCommand}"
                    Padding="18,6"
                    Margin="0,0,12,0"/>
            <Button Grid.Column="3"
                    Content="Download"
                    Command="{Binding DownloadPlcProgramCommand}"
                    Padding="18,6"
                    Margin="0,0,12,0"/>
            <Button Grid.Column="4"
                    Content="Erase"
                    Command="{Binding ErasePlcProgramCommand}"
                    Padding="18,6"
                    Margin="0,0,12,0"/>
            <TextBlock Grid.Column="5"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                       TextWrapping="Wrap"
                       Text="{Binding PlcHintMessage}"/>
        </Grid>

        <Border Grid.Row="1"
                Margin="0,0,0,10"
                Padding="10"
                Background="#1AFFFFFF"
                CornerRadius="6">
            <TextBlock Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                       TextWrapping="Wrap"
                       Text="{Binding PlcStatusMessage}"/>
        </Border>

        <TextBox Grid.Row="2"
                 Text="{Binding PlcProgramSource, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 FontFamily="Consolas"
                 FontSize="13"
                 IsReadOnly="{Binding IsPlcProgramBusy}"
                 Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                 Background="#0F000000"/>
    </Grid>
</UserControl>